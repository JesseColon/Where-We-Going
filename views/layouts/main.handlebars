<!-- main.handlebars -->

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Local Event Finder</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css"/>
  <link rel="stylesheet" href="/css/jass.css">
</head>

<body>
  <header class="display-flex justify-space-between align-center p-2">
    <h1>
      Find a Local Event
    </h1>
    {{#if logged_in}}
    <nav>
      <a href="../partials/event-block.handlebars">Events</a>
      <button class="btn" id="logout">Logout</button>
    </nav>
    {{/if}}
  </header>
  <main class="container container-fluid mt-5">
    <section class="section">
      <div class="container">
        <h2 class="title">Find a Local Event</h2>
        <p class="subtitle">Search for exciting events happening in your area.</p>
        <!-- Add your event search form or content here -->
      </div>
    </section>
    {{{ body }}}
  </main>
  <footer>
    <!-- Footer content goes here -->
  </footer>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>

